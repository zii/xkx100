# 战斗经验系统数值设计

## 核心公式

```
reward_base = max(5, sqrt(npc_exp) * 2 / 5)
```

每命中一次 NPC，玩家获得 `reward_base` 点 combat_exp 和动态潜能量 `reward_base * pm / 100`。

```
pm = 200 - pot * 100 / exp      // 范围 100~200
pot << exp → pm ≈ 200 → 2倍
pot ≈ exp  → pm ≈ 100 → 1倍
```

## 触发时机（do_attack 中 4 个点）

| 动作 | 条件 | 原奖励 | 新奖励 |
|------|------|--------|--------|
| 躲闪成功 | dp < ap, 至少一方 NPC | combat_exp += 1 | combat_exp += reward_base |
| 招架成功 | dp < ap, 至少一方 NPC | combat_exp += 1 | combat_exp += reward_base |
| 命中对方(以弱胜强) | ap < dp, 玩家攻NPC | combat_exp += 1, potential += 1 | combat_exp += reward_base, potential += reward_base*2 |
| 被命中(伤害够大) | random(2) 50% | combat_exp += 1, potential += 1 | combat_exp += reward_base, potential += reward_base*2 |

所有条件判断保持不变，仅放大数值。

例外：被命中奖励的原条件 `random(max_qi + qi) < damage` 触发率极低（满血时仅 ~5%），改为 `random(2)` 固定 50% 概率，确保被命中方也能稳定获得奖励。

## 击杀奖励（killer_reward 中新增）

```
kill_bonus = sqrt(npc_exp) / 20
```

击杀奖仅占总收益 1.5-2.5%，鼓励 fight 不鼓励 kill。

## 全阶段数值

| 当前经验 | 同级 NPC | 每次命中 | 10次命中 | 击杀奖 | **每场合计** | **翻倍所需场次** |
|---------|---------|---------|---------|-------|-----------|------------|
| 1k | 1k | 12 | 120 | 1 | **121** | **8** |
| 5k | 5k | 28 | 280 | 3 | **283** | **18** |
| 10k | 10k | 40 | 400 | 5 | **405** | **25** |
| 20k | 20k | 56 | 560 | 7 | **567** | **35** |
| 50k | 50k | 89 | 890 | 11 | **901** | **55** |
| 100k | 100k | 126 | 1,260 | 15 | **1,275** | **78** |
| 200k | 200k | 178 | 1,780 | 22 | **1,802** | **111** |
| 500k | 500k | 282 | 2,820 | 35 | **2,855** | **175** |
| 1M | 1M | 400 | 4,000 | 50 | **4,050** | **247** |
| 5M | 5M | 894 | 8,940 | 111 | **9,051** | **552** |
| 10M | 10M | 1,264 | 12,640 | 158 | **12,798** | **781** |
| 30M | 30M | 2,190 | 21,900 | 273 | **22,173** | **1,353** |

## 翻倍场次曲线

8 → 18 → 25 → 35 → 55 → 78 → 111 → 175 → 247 → 552 → 781 → 1353

新手快、老手慢，逐级平滑过渡。

## 不影响的部分

- 现有 quest 奖励机制不变，大小不变
- 所有条件判断（ap/dp、谁得奖励、何时触发）不变
- 技能上限公式（skill³/10 ≤ combat_exp）不变
- practice/learn 系统不变
- NPC 战斗经验表（npcd.c）不变
- 登录更新、角色属性计算不变
